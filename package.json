{
  "name": "crown-accumulator-website",
  "version": "1.0.0",
  "scripts": {
    "server": "npm run dev -w server",
    "client": "npm run dev -w client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build -w client"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
